- var posts = public.drafts._data
- var data = partial('../_partials/data-loop', { collection: posts })

block content
  h2 Drafts
  div.centered.blog
    each item in data
      h3
        a(href='/drafts/'+item.slug) #{item.title}
