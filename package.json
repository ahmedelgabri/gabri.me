{
  "name": "gabri.me",
  "version": "1.0.0",
  "description": "Some guy on the web",
  "main": "server.js",
  "scripts": {
    "test": "harp compile",
    "build": "node ./server.js compile",
    "server": "node ./server.js",
    "watch": "nodemon -w ./public -e js,jade,styl ./server.js",
    "start": "npm run build && npm run watch",
    "clean": "rm -rf www/",
    "predeploy": "npm run clean && npm run build",
    "deploy": "surge -p ./www -d gabrime.surge.sh"
  },
  "private": true,
  "author": "Ahmed El Gabri <ahmed@gabri.me>",
  "license": "MIT",
  "dependencies": {
    "glob": "^4.0.6",
    "harp": "^0.17.0",
    "moment": "^2.8.3",
    "router-stupid": "^0.7.0",
    "st": "^0.5.2"
  },
  "engines": {
    "node": "0.12.x"
  },
  "devDependencies": {
    "nodemon": "^1.3.7"
  }
}
