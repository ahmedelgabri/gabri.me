{
  "name": "gabri.me",
  "version": "1.0.0",
  "description": "Some guy on the web",
  "main": "server.js",
  "scripts": {
    "test": "harp compile",
    "build": "node ./server.js compile",
    "server": "node ./server.js",
    "watch": "nodemon -w ./public -e js,jade,styl,md ./server.js",
    "dev": "npm run build && npm run watch",
    "start": "npm run build && npm run server",
    "clean": "rm -rf www/",
    "predeploy": "npm run clean && npm run build"
  },
  "private": true,
  "author": "Ahmed El Gabri <ahmed@gabri.me>",
  "license": "MIT",
  "dependencies": {
    "glob": "^5.0.14",
    "harp": "^0.19.0",
    "jstransformer-marked": "^1.0.1",
    "moment": "^2.10.6",
    "router-stupid": "^0.7.0",
    "st": "^0.5.5"
  },
  "devDependencies": {
    "nodemon": "^1.5.1"
  },
  "engines": {
    "node": "0.12.7"
  }
}
