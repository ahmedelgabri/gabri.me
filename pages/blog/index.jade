- var posts = public.blog._data
- var data = partial('../_partials/data-loop', { collection: posts })

block content
  h2 #{public._data[current.path[0]].title}
  div.centered.blog
    each item in data
      p
        a(href='/blog/'+item.slug) #{item.title}
